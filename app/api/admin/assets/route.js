import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Admin Assets API — Coming Soon' });
}

export async function POST() {
  return NextResponse.json({ message: 'Admin Assets API — Coming Soon' });
}
